#include<iostream>
#include<cmath>
using namespace std;

/*
int main() {
	int a, da, b, db;
	int tmp1, tmp2,count1,count2;
	int sum1, sum2, sum;
	sum1 = sum2 = sum = 0;
	count1 = count2 = 0;

	scanf("%d %d %d %d", &a, &da, &b, &db);
	while (a){
		int t = a % 10;
		a /= 10;
		if (t == da) {
			count1++;
		}
	}
	while (b) {
		int t = b % 10;
		b /= 10;
		if (t == db) {
			count2++;
		}
	}

	//printf("%d %d %d %d\n", da, db, count1, count2);

	for (int i = 0; i < count1; i++) {
		sum1 += da * pow(10, i);
	}
	//printf("%d\n", sum1);

	for (int i = 0; i < count2; i++) {
		sum2 += db * pow(10, i);
	}
	//printf("%d\n", sum2);

	sum = sum1 + sum2;
	printf("%d\n", sum);
	return 0;

}
*/
